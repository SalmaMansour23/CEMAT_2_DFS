Start button di output starts C_DRV. Stop button di output stops it. Analog input c_measur output is an input to the motor,
controlling it based on its value. The 3 inputs are independent of each other but all feed into the motor.